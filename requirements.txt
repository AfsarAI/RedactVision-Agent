# =============================================================================
# RedactVision Agent — Python Backend Dependencies
# Install with: pip install -r requirements.txt (or cd server && pip install -e ".[dev]")
# Requires: Python >= 3.10
# =============================================================================

# Core Server Framework & Transport
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9

# HTTP Client & LLM Provider Transport
httpx>=0.27.0
python-dotenv>=1.0.0

# Server Development & Testing (Optional)
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Editable server package install
-e ./server

# =============================================================================
# Extension Dependencies (Node.js / npm)
# Location: extension/package.json
# Install with: cd extension && npm install && npm run build
#
# Build Tools:
# - esbuild@^0.28.2
# - typescript@^7.0.2
# - @types/chrome@^0.2.7
#
# Optional On-Device Perception:
# - @huggingface/transformers@^3.0.0
# - tesseract.js@^5.0.0
# =============================================================================